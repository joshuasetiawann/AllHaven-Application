# v0.14.0 — Terminal-only install (browser wizard now opt-in) + faster Docker check

**Date:** 2026-06-11 · **Type:** Minor · **Archive:** `AllHaven 2.6` on `master`.

Reverts the install flow to **terminal-only**. The browser Setup Wizard introduced in
v0.13.0 had recurring problems — the local setup server erroring on stop, and a slow
Docker-detection step — so it is no longer the default. Installing and running Haven
now happens entirely in the terminal again (the proven `haven_cli.py` flow), with live
Docker/`pip`/`npm` progress. The wizard remains available, opt-in, for anyone who wants it.

## What changed

- **Terminal-only by default.** `START_HAVEN_*`, `./install.sh`, and `npm run setup`
  all run `installer/haven_cli.py`: tool checks → `.env` (with backup) → `docker compose
  up -d postgres` (live pull) → backend venv + `pip install` + `alembic upgrade` →
  frontend `npm install` → start services → open the app. No website involved.
- **Launchers branch on setup state:**
  - first run (no `.env`) → terminal installer (`haven_cli.py`).
  - already configured → `haven_launch.py` (ensure services running → open app). The
    **desktop shortcut** uses this, so it opens Haven without re-installing.
  - `HAVEN_FORCE_SETUP=1` re-runs the installer; `HAVEN_SETUP_WEB=1` opens the optional
    browser wizard instead.
- **Faster Docker check.** `docker_running()` / `compose_available()` timeouts dropped
  from **8s → 4s**. The terminal path probes the daemon **once**; the web wizard's
  repeated multi-probe detection (the slow part users hit) is no longer in the default
  flow.
- **Browser wizard kept, opt-in.** `installer/haven_setup.py` and its `/api/*` endpoints
  are unchanged and still work via `HAVEN_SETUP_WEB=1` — nothing was deleted.

## Files changed

`START_HAVEN_{WINDOWS.bat,MAC.command,LINUX.sh}` and `install.sh` (default → terminal
installer; `HAVEN_SETUP_WEB=1` for the wizard), `installer/run.js` + `package.json`
(`npm run setup` → terminal; `npm run setup:web` → wizard), `installer/haven_common.py`
(Docker probe 8s → 4s), docs (README, `docs/DESKTOP_SETUP.md`) + version bump.

## Testing

- Installer logic: `pytest installer/tests` → **15 passed**; all installer modules
  `py_compile` + import clean; `run.js` / `package.json` validated by node;
  `START_HAVEN_*` + `install.sh` pass `bash -n`.
- Backend **168 passed**; frontend `tsc` + `next build` clean (no app-code change).
- **Not runnable here (honest):** the live `docker compose up` + `pip`/`npm` install
  end-to-end (no Docker daemon in the sandbox). Verified by construction + unit/syntax
  checks. Manual steps below.

## How to verify

1. Fresh clone (no `.env`): `./START_HAVEN_LINUX.sh` (or `./install.sh`, or
   `npm run setup`). Everything installs **in the terminal** with live progress; the
   app opens at the end. The Docker check returns within ~4s.
2. Re-run with `.env` present → it skips install and just starts + opens the app
   (this is what the desktop shortcut does). `HAVEN_FORCE_SETUP=1` re-runs the installer.
3. Optional browser wizard: `HAVEN_SETUP_WEB=1 ./START_HAVEN_LINUX.sh`.

## Known limitations

- First-run install needs network access (Docker image, `pip`, `npm`).
- The opt-in browser wizard's known issues (server-stop error, slower detection) are
  not fixed here — it's simply no longer the default path.
