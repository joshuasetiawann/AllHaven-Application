@echo off
REM AllHaven first-time setup (Windows). The Linux/macOS equivalent is ./install.sh.
REM Prefer AllHaven-Setup.exe if you have it - it needs no Python installed.
"%~dp0setup\windows\Install-AllHaven.bat" %*
