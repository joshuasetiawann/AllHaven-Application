# Capacitor-generated web assets — regenerated by `npx cap sync` (which runs the
# static export) before every build. Never commit a baked snapshot (it would
# pin a stale build and a hard-coded NEXT_PUBLIC_API_BASE_URL).
public/
capacitor.config.json
